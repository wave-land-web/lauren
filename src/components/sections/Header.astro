---
import { Picture } from 'astro:assets'
import Navigation from './Navigation.astro'
import curvedName from '../../assets/curved-name.svg'
import headshot from '../../assets/headshot.png'
import RotatingTitles from '../animations/RotatingTitles.astro'
---

<header>
  <Navigation />
  <div class="content aos-fade">
    <div class="signature">
      <Picture
        src={curvedName}
        formats={['avif', 'webp']}
        alt="Curved name text"
        loading="eager"
        class="image-one"
      />
      <Picture
        src={headshot}
        formats={['avif', 'webp']}
        alt="Lauren Vogelstein Headshot"
        loading="eager"
        class="image-two"
      />
    </div>
    <div class="info">
      <RotatingTitles />
      <div class="description">
        <p>
          I'm am an Assistant Professor of Technology, Media, & Learning at Teachers College,
          Columbia University.
        </p>
        <p>
          I design and study STEAM learning environments where the A in STEAM is as respected as the
          STEM disciplines involved. In particular, I explore how collaborating with dancers and
          choreographers on the design and analysis of STEAM spaces can leverage artistic,
          choreographic research as rigorous forms of collective learning. I have extended this
          research as a Co-PI on the Choreographing Science Project, an NSF AISL grant (Award
          #2115773).
        </p>
      </div>
    </div>
  </div>
</header>

<style>
  header {
    position: relative;
    background: var(--color-blue);
  }

  p {
    font-size: clamp(var(--size-sm), 5vw, var(--size-base));
  }

  @media (min-width: 1024px) {
    .content {
      max-width: var(--max-width);
      margin: 0 auto;
      display: flex;
      justify-content: space-evenly;
      gap: 2rem;
    }
  }

  .signature {
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    align-items: center;
  }

  .signature .image-one {
    width: 90%;
    max-width: 450px;
  }

  @media (max-height: 700px) {
    .signature .image-one {
      max-width: 39vh;
    }
  }

  .signature .image-two {
    max-width: 300px;
  }

  @media (max-height: 700px) {
    .signature .image-two {
      max-width: 39vh;
    }
  }

  .info {
    align-self: center;
    padding: 4rem 2rem;
    text-align: center;
    background-color: var(--color-white);
  }

  @media (min-width: 1024px) {
    .info {
      padding: 4rem 1rem;
      text-align: left;
      color: var(--color-white);
      background-color: transparent;
    }
  }

  .description {
    max-width: 500px;
    margin: 0 auto;
  }

  .description p:not(:last-of-type) {
    margin-bottom: 1rem;
    font-size: var(--size-md);
  }
</style>

<script>
  function handleFadeInOnLoad() {
    const content = document.querySelector('.content')
    content?.classList.add('active')
  }

  window.addEventListener('load', handleFadeInOnLoad)
</script>
